d, while Node has a nullable field.
  set text(covariant String value) {
    data = value;
  }
}

// TODO(jmesserly): Elements should have a pointer back to their document
class Element extends Node with _ParentNode, _ElementAndDocument {
  final String? namespaceUri;

  /// The [local name](http://dom.spec.whatwg.org/#concept-element-local-name)
  /// of this element.
  final String? localName;

  // TODO(jmesserly): consider using an Expando for this, and put it in
  // dom_parsing. Need to check the performance affect.
  /// The source span of the end tag this element, if it was created by the
  /// [HtmlParser]. May be `null` if does not have an implicit end tag.
  FileSpan? endSourceSpan;

  Element._(this.localName, [this.namespaceUri]) : super._();

  Element.tag(this.localName)
      : namespaceUri = Namespaces.html,
        super._();

  static final _startTagRegexp = RegExp('<(\\w+)');

  static final _customParentTagMap = const {
    'body': 'html',
    'head': 'html',
    'caption': 'table',
    'td': 'tr',
    'colgroup': 'table',
    'col': 'colgroup',
    'tr': 'tbody',
    'tbody': 'table',
    'tfoot': 'table',
    'thead': 'table',
    'track': 'audio',
  };

  // TODO(jmesserly): this is from dart:html _ElementFactoryProvider...
  // TODO(jmesserly): have a look at fixing some things in dart:html, in
  // particular: is the parent tag map complete? Is it faster without regexp?
  // TODO(jmesserly): for our version we can do something smarter in the parser.
  // All we really need is to set the correct parse state.
  factory Element.html(String html) {
    // TODO(jacobr): this method can be made more robust and performant.
    // 1) Cache the dummy parent elements required to use innerHTML rather than
    //    creating them every call.
    // 2) Verify that the html does not contain leading or trailing text nodes.
    // 3) Verify that the html does not contain both <head> and <body> tags.
    // 4) Detach the created element from its dummy parent.
    var parentTag = 'div';
    String? tag;
    final match = _startTagRegexp.firstMatch(html);
    if (match != null) {
      tag = match.group(1)!.toLowerCase();
      if (_customParentTagMap.containsKey(tag)) {
        parentTag = _customParentTagMap[tag]!;
      }
    }

    final fragment = parseFragment(html, container: parentTag);
    Element element;
    if (fragment.children.length == 1) {
      element = fragment.children[0];
    } else if (parentTag == 'html' && fragment.children.length == 2) {
      // You'll always get a head and a body when starting from html.
      element = fragment.children[tag == 'head' ? 0 : 1];
    } else {
      throw ArgumentError('HTML had ${fragment.children.length} '
          'top level elements but 1 expected');
    }
    element.remove();
    return element;
  }

  @override
  int get nodeType => Node.ELEMENT_NODE;

  // TODO(jmesserly): we can make this faster
  Element? get previousElementSibling {
    if (parentNode == null) return null;
    final siblings = parentNode!.nodes;
    for (var i = siblings.indexOf(this) - 1; i >= 0; i--) {
      final s = siblings[i];
      if (s is Element) return s;
    }
    return null;
  }

  Element? get nextElementSibling {
    final parentNode = this.parentNode;
    if (parentNode == null) return null;
    final siblings = parentNode.nodes;
    for (var i = siblings.indexOf(this) + 1; i < siblings.length; i++) {
      final s = siblings[i];
      if (s is Element) return s;
    }
    return null;
  }

  @override
  String toString() {
    final prefix = Namespaces.getPrefix(namespaceUri);
    return "<${prefix == null ? '' : '$prefix '}$localName>";
  }

  @override
  String get text => _getText(this);

  @override
  set text(String? value) => _setText(this, value);

  /// Returns a fragment of HTML or XML that represents the element and its
  /// contents.
  String get outerHtml => _outerHtml;

  /// Returns a fragment of HTML or XML that represents the element's contents.
  /// Can be set, to replace the contents of the element with nodes parsed from
  /// the given string.
  String get innerHtml => _innerHtml;

  // TODO(jmesserly): deprecate in favor of:
  // <https://api.dartlang.org/apidocs/channels/stable/#dart-dom-html.Element@id_setInnerHtml>
  set innerHtml(String value) {
    nodes.clear();
    // TODO(jmesserly): should be able to get the same effect by adding the
    // fragment directly.
    nodes.addAll(parseFragment(value, container: localName!).nodes);
  }

  @override
  void _addOuterHtml(StringBuffer str) {
    // http://www.whatwg.org/specs/web-apps/current-work/multipage/the-end.html#serializing-html-fragments
    // Element is the most complicated one.
    str.write('<');
    str.write(_getSerializationPrefix(namespaceUri));
    str.write(localName);

    if (attributes.isNotEmpty) {
      attributes.forEach((key, v) {
        // Note: AttributeName.toString handles serialization of attribute
        // namespace, if needed.
        str.write(' ');
        str.write(key);
        str.write('="');
        str.write(htmlSerializeEscape(v, attributeMode: true));
        str.write('"');
      });
    }

    str.write('>');

    if (nodes.isNotEmpty) {
      if (localName == 'pre' ||
          localName == 'textarea' ||
          localName == 'listing') {
        final first = nodes[0];
        if (first is Text && first.data.startsWith('\n')) {
          // These nodes will remove a leading \n at parse time, so if we still
          // have one, it means we started with two. Add it back.
          str.write('\n');
        }
      }

      _addInnerHtml(str);
    }

    // void elements must not have an end tag
    // http://dev.w3.org/html5/markup/syntax.html#void-elements
    if (!isVoidElement(localName)) str.write('</$localName>');
  }

  static String _getSerializationPrefix(String? uri) {
    if (uri == null ||
        uri == Namespaces.html ||
        uri == Namespaces.mathml ||
        uri == Namespaces.svg) {
      return '';
    }
    final prefix = Namespaces.getPrefix(uri);
    // TODO(jmesserly): the spec doesn't define "qualified name".
    // I'm not sure if this is correct, but it should parse reasonably.
    return prefix == null ? '' : '$prefix:';
  }

  @override
  Element clone(bool deep) {
    final result = Element._(localName, namespaceUri)
      ..attributes = LinkedHashMap.from(attributes);
    return _clone(result, deep);
  }

  // http://dom.spec.whatwg.org/#dom-element-id
  String get id {
    final result = attributes['id'];
    return result ?? '';
  }

  set id(String value) {
    attributes['id'] = value;
  }

  // http://dom.spec.whatwg.org/#dom-element-classname
  String get className {
    final result = attributes['class'];
    return result ?? '';
  }

  set className(String value) {
    attributes['class'] = value;
  }

  /// The set of CSS classes applied to this element.
  ///
  /// This set makes it easy to add, remove or toggle the classes applied to
  /// this element.
  ///
  ///     element.classes.add('selected');
  ///     element.classes.toggle('isOnline');
  ///     element.classes.remove('selected');
  CssClassSet get classes => ElementCssClassSet(this);
}

class Comment extends Node {
  String? data;

  Comment(this.data) : super._();

  @override
  int get nodeType => Node.COMMENT_NODE;

  @override
  String toString() => '<!-- $data -->';

  @override
  void _addOuterHtml(StringBuffer str) {
    str.write('<!--$data-->');
  }

  @override
  Comment clone(bool deep) => Comment(data);

  @override
  String? get text => data;

  @override
  set text(String? value) {
    data = value;
  }
}

// TODO(jmesserly): fix this to extend one of the corelib classes if possible.
// (The requirement to remove the node from the old node list makes it tricky.)
// TODO(jmesserly): is there any way to share code with the _NodeListImpl?
class NodeList extends ListProxy<Node> {
  final Node _parent;

  NodeList._(this._parent);

  Node _setParent(Node node) {
    // Note: we need to remove the node from its previous parent node, if any,
    // before updating its parent pointer to point at our parent.
    node.remove();
    node.parentNode <svg width="20" height="21" viewBox="0 0 20 21" fill="none" xmlns="http://www.w3.org/2000/svg">
<g id="ic:round-plus">
<path id="Vector" d="M15.1429 11.3571H10.8571V15.6429C10.8571 15.8702 10.7668 16.0882 10.6061 16.2489C10.4453 16.4097 10.2273 16.5 10 16.5C9.77267 16.5 9.55465 16.4097 9.39391 16.2489C9.23316 16.0882 9.14286 15.8702 9.14286 15.6429V11.3571H4.85714C4.62981 11.3571 4.4118 11.2668 4.25105 11.1061C4.09031 10.9453 4 10.7273 4 10.5C4 10.2727 4.09031 10.0547 4.25105 9.89391C4.4118 9.73316 4.62981 9.64286 4.85714 9.64286H9.14286V5.35714C9.14286 5.12981 9.23316 4.9118 9.39391 4.75105C9.55465 4.59031 9.77267 4.5 10 4.5C10.2273 4.5 10.4453 4.59031 10.6061 4.75105C10.7668 4.9118 10.8571 5.12981 10.8571 5.35714V9.64286H15.1429C15.3702 9.64286 15.5882 9.73316 15.7489 9.89391C15.9097 10.0547 16 10.2727 16 10.5C16 10.7273 15.9097 10.9453 15.7489 11.1061C15.5882 11.2668 15.3702 11.3571 15.1429 11.3571Z" fill="#308BF9"/>
</g>
</svg>
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
<g id="la:brain">
<g id="Group 623">
<path id="Vector" d="M20.833 17.2883L20.8325 17.2906C20.8301 17.3013 20.8255 17.3222 20.8216 17.3425C20.8169 17.3677 20.811 17.4036 20.8084 17.4455L20.8066 17.4759L20.8084 17.5063C20.8094 17.5234 20.811 17.5395 20.8129 17.5544L20.8091 17.6001C20.6647 19.3772 19.2535 20.75 17.5833 20.75H17.5317H17.2204L17.083 21.0293C16.6457 21.9185 15.9471 22.4998 15.0838 22.5C14.7656 22.4991 14.4501 22.4314 14.1558 22.3C13.9169 22.1933 13.6953 22.0462 13.5 21.8641V18.3333V10.75V5.5V3.41735C14.022 2.85076 14.728 2.5 15.5 2.5C16.8178 2.5 17.931 3.50985 18.2135 4.88898L18.2809 5.2179L18.6109 5.28C20.3246 5.60254 21.6667 7.10352 21.6667 9C21.6667 9.18005 21.6482 9.34268 21.62 9.52481L21.591 9.71284L21.6943 9.87262C22.1878 10.6358 22.5 11.5203 22.5 12.5C22.5 13.9294 21.8158 15.1471 20.818 16.0373L20.5952 16.2361L20.6645 16.5265C20.6849 16.6119 20.7076 16.6938 20.728 16.7668L20.7321 16.7815C20.7515 16.851 20.7686 16.9119 20.7837 16.9729C20.8132 17.0912 20.8305 17.191 20.833 17.2883Z" fill="#2365B5" stroke="#308BF9"/>
<path id="Vector_2" d="M3.33384 17.2837C3.33664 17.2951 3.34136 17.3146 3.3453 17.3335C3.35076 17.3598 3.3576 17.3978 3.36061 17.4424L3.36287 17.4759L3.36065 17.5093C3.35956 17.5256 3.35797 17.541 3.35612 17.5553L3.35977 17.5961C3.51524 19.3354 5.05189 20.75 6.95833 20.75H7.01517H7.31315L7.45494 21.0121C7.92918 21.8887 8.707 22.4999 9.70807 22.5C10.0719 22.4991 10.4313 22.4286 10.7648 22.2932L10.9508 22.7514L10.7648 22.2932C11.0347 22.1837 11.2828 22.0336 11.5 21.8499V18.3333V10.75V5.5V3.43123C10.9223 2.86298 10.1301 2.5 9.25 2.5C7.73829 2.5 6.51864 3.54724 6.2141 4.89855L6.14053 5.22502L5.81066 5.28146C3.87171 5.61321 2.41667 7.13958 2.41667 9C2.41667 9.17644 2.43659 9.33632 2.46735 9.51738L2.5021 9.72189L2.38186 9.89093C1.83907 10.6541 1.5 11.5322 1.5 12.5C1.5 13.9104 2.24289 15.1249 3.34904 16.022L3.59966 16.2253L3.51768 16.5374C3.49504 16.6236 3.46982 16.7062 3.44741 16.7791L3.44293 16.7937C3.42153 16.8633 3.40295 16.9238 3.38643 16.9842C3.35509 17.0987 3.33703 17.1932 3.33384 17.2837Z" fill="#308BF9" stroke="#2365B5"/>
</g>
</g>
</svg>
                         